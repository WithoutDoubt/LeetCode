```python
fluid.data # 创建数据

import paddle.fluid as fluid
x = fluid.data(name = 'x', shape=[3,None],dtype="int64")

batch_x = fluid.data(name="batched_x",shape=[None,3,None],dtype = 'int64')



######################## 创建常量
import paddle.fluid as fluid
data = fluid.layers.fill_constant(shape=[3,4],value=16,dtype='int64')
```

